[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
glam = "0.30.9"
tokio = { version = "1.48.0", features = ["full"] }
voxel-core = { version = "0.1.0", path = "../voxel-core" }
voxel-world = { version = "0.1.0", path = "../voxel-world" }
